FROM golang:1.22.0-alpine
LABEL authors="Ckala"
WORKDIR /src

# COPY go.mod go.sum .

ENV CGO_ENABLED=0

RUN apk update
RUN apk add gcompat
RUN apk add mc

RUN cd /src go mod tidy



# RUN go build -o ./cmd/app ./cmd/main.go
# CMD ["go build -o ./cmd/app ./cmd/main.go"]
CMD ["./cmd/app"]
